"use client";
import { FormEvent, useState } from "react";
export default function Booking() { const [sent, setSent] = useState(false); const submit = (e: FormEvent) => { e.preventDefault(); setSent(true); }; return <section className="booking section-grid" id="booking"><div><p className="eyebrow">004 / INITIATE CONTACT</p><h2>Bring the<br /><span>frequency.</span></h2><p className="body-copy">For bookings, collaborations, and other transmissions.</p></div><form onSubmit={submit}>{sent ? <div className="success">Transmission received.<br /><small>We&apos;ll be in touch.</small></div> : <><label>Name<input required placeholder="Your name" /></label><label>Email<input required type="email" placeholder="you@email.com" /></label><label>Tell us about it<textarea required placeholder="City, date, vision..." /></label><button className="button button-orange" type="submit">Send transmission <span>↗</span></button></>}</form></section>; }
